# import pyttsx3
name = "My name   is Naveed"

# engine = pyttsx3.init()
# engine.say(name)
# engine.runAndWait()
print(name)
print(name.find("  "))
print(name.replace("  ", " "))


# Strings are immutable which means the original string will remain the same 